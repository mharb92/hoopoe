# Hoopoe (الهدهد)

Palestinian Arabic learning PWA. Clean rebuild, spec phase. No app code yet.

- Rebuild base: this repo.
- Read-only harvest source: `mharb92/arabic-app` @ `modular-rebuild-5` @ `7dc0b14`. Never patched, never built on.
- Working docs in `docs/`. Spec, tracker and principles live in the claude.ai project, not here.
- DR (dictionary review) docs: `docs/dr/`. Harness: `tools/dr/`.

See `CLAUDE.md` before any session.
