# Hoopoe spec tracker
Updated 2026-09-13 · chat 22

## Session rules
In `CLAUDE.md` at the repo root, not here. This file holds state only: agenda, decisions, bug history, refactor history, open questions, parked items. Where the two ever disagree, `CLAUDE.md` wins.

## Agenda
| item | status |
|---|---|
| A Kickoff | approved |
| B Product | approved |
| C1 Journey | approved |
| C2 Placement | approved |
| C3 Plan | approved |
| C4 Lesson | approved |
| C5 Generation | approved |
| C6 Mastery | approved |
| C7 Quizzes | approved |
| C8 Checkpoints | approved |
| C9 Audio | approved |
| C10 Tracks | approved |
| C11 Side surfaces | approved |
| Retro-audit A1-A7 (chat 11) | applied to B, C2, C4, C5, C7, C9 |
| DR Dictionary review, **scoped for MVP** (D141) | specced (chat 12: `dr-spec.md`, `dr-prompt.md`, `dr-runbook.md`) · schema, migration and `hoopoe-dr` environment done (chat 14) · runner specced chat 15 (`dr-runner-spec.md`) · scoped chat 16 (`dr-scoped-pass.md`, `dr-prompt-scoped.md`): one unattended pass for `level`, romanization, `arabic_vocalised` and the enums, every human queue deferred to a named gate · build B1-B3 then the pilot, gated on P1-P10 · **B1-B2 built and merged to `main` (chat 18, chat 22 PR #8): 74 `node:test` cases pass, 43 at B1 and 31 added at B2.** `tools/dr/` carries `config`, `sample`, `rulefix`, `prompt`, `prompt-content`, `validate`, `route`, `db`, `judge`, `stream`; `docs/dr/romanization-map.md` is on `main`. B3-B4 not started, nothing blocked · runs in the background alongside D-J, never blocking them |
| D UX | approved (chats 17-18, D142-D209) |
| §D internal review (chat 18) | applied: D164, D165, D178, D186 revised; D198, D199 added; onboarding, wrap-up and alphabet specced D200-D209 |
| E Architecture | **block 1 of 3 approved (chat 22, D212-D223): stack and structure.** Blocks 2 (runtime shape) and 3 (build-time and portability) open; then the internal review and the principles check. Prose goes into `PROJECT_SPEC.md` when block 3 closes, as §D did |
| F Data · G Integrations/security · H Quality/delivery · I Roadmap proof · J Manual actions | todo |
| Phase 2 build package | blocked (needs full spec approval) |

## Inputs
harvest.md, harvest-prompts.md (`modular-rebuild-5` @ `7dc0b14`), schema-snapshot.md, principles.md present. Dictionary: 2,728 rows, readable via anon key on the old project `xkhulybdrxdzakarivvi` until DR promotes. The DR target is the new project `pniwgnjljpkiimssortp` (D137); every figure below was measured on the old one and carries over unchanged, ids included. `rank` = generation batch order, meaningful only within a category (chat 3), retired as a difficulty signal (D127). Dictionary shape (chat 12): pos Noun 979 / Phrase 879 / Verb 352 / Adjective 235 / Expression 87; `dialect_tag` D 1277 / M 1119 / S 210 / SL 63 / MSA 50 / D-var 9; confidence 5 × 2,422, 4 × 272, 3 × 34; 2,602 rows eligible under the C2.2 filter.

## A: user feedback (Marwan)
Principles right at a high level; implementation haphazard, bogged down by old code; UX, placement and the core loop never worked. Rebuild must prioritise correct architecture, extensibility, best practice. Matches harvest §8.

## Decision log
| id | decision | options considered | bucket | door | approved |
|---|---|---|---|---|---|
| D0 | "Decisions already made" in session brief | — | — | — | Y |
| D1 | Skill/spec/tracker updates are scoped to the affected sections and delivered as complete final files, never as diffs (clarified chat 6) | full rewrite / section edits | process | two-way | Y |
| D2 | Vision statement (spec B1) | — | MVP | two-way | Y |
| D3 | Personas: heritage + beginner MVP, intermediate slot | — | MVP/slot | two-way | Y |
| D4 | MVP list unchanged from brief | — | MVP | two-way | Y |
| D5 | 5-7 min target for all learner types | shorter beginner target / longer beginner horizon | MVP | two-way | Y |
| D6 | Phase fixed 90 days; plan stores `duration_days` | user picks 30/60/90/180 / longer beginner default | MVP + slot | one-way (field) | Y |
| D7 | Other dialects = future tracks | ignore / MVP picker | slot | two-way | Y |
| D8 | Payments tentative roadmap; slot = per-feature access rule; MVP family only | ignore / billing in MVP | slot | two-way | Y |
| D9 | Success criteria, 4-week family test (spec B8) | original 5-10 testers, 2 checkpoints | MVP | two-way | Y |
| D10 | Out-of-scope list (spec B7) | — | ignore | two-way | Y |
| D11 | Native App Store app on roadmap; must be wrapper or port, not rewrite | out of scope | slot | one-way (framework, E) | Y |
| D12 | Sign in before onboarding; no guest mode | onboarding first | MVP | two-way | Y |
| D13 | Behavioural learner-type question; placement may reassign | label pick | MVP | two-way | Y |
| D14 | Quiz mandatory in session; exit/resume allowed | skippable | MVP | two-way | Y |
| D15 | No daily session cap; catch-up and working ahead allowed; progress by completed lessons; cost not a pacing lever. Narrowed by D63 | 1/day + extra practice / cap set in C5 | MVP | two-way | Y (Marwan override) |
| D16 | Beginner script woven into sessions via config; Alphabet not a gate | standalone / gate | MVP | two-way | Y |
| D17 | Streak = any completed activity; silent reset; longer review after 7+ days away | lessons only | MVP | two-way | Y |
| D18 | Phase end auto-starts next 90-day phase; renewal flow replaces later | dead-end screen | MVP + slot | two-way | Y |
| D19 | Answers queue on device when offline; new content needs connection | block with retry | MVP | one-way (sync, E) | Y |
| D20 | All learner types take placement; beginner run is a ~1 min warm-up (closes Q6) | skip / self-select | MVP | two-way | Y |
| D21 | Adaptive band staircase; bands by dictionary `level` | fixed rounds / IRT / rank bands | MVP | two-way | Y |
| D22 | Pre-built, native-reviewed placement item bank; stable ids; no runtime Claude | runtime from rows / Claude-generated | MVP | one-way (item ids, F) | Y |
| D23 | Placement outputs: ceiling, listening accuracy, `script_stage`, production ratio | keep 6 dims | MVP | two-way | Y |
| D24 | Production via self-rating on correct Listen items | typed + fuzzy / none | MVP | two-way | Y |
| D25 | Auto-reassign learner type by ceiling; one-tap undo at plan reveal | confirm screen / none | MVP | two-way | Y |
| D26 | Script micro-steps and scaffolds key off `script_stage`, not learner type (amends C1.2) | beginner-only | MVP | two-way | Y |
| D27 | Seed vocab store with correct placement items only; "already knew" excluded from words learned. Extended by D66 | all tested at 80/20 | MVP | two-way | Y |
| D28 | No retake UI; attempts append-only with `attempt_no` + config version | retake in Settings | MVP + slot | two-way | Y |
| D29 | Placement UX rules (spec C2.8) | per-item feedback | MVP | two-way | Y |
| D30 | Dictionary gains `level` 1-5 in DR; fallback within-category rank percentile | rank bands | MVP | one-way (column, F) | Y |
| D31 | Plan = ordered units; dates display only | day-by-day calendar / rolling re-plan | MVP | one-way (unit rows, F) | Y |
| D32 | Pre-built unit catalogue + ordering rules; no Claude call to plan | Claude-written outline per learner / catalogue + Claude ordering | MVP | one-way (unit ids) | Y |
| D33 | Goal tags = onboarding options; free text mapped by one validated Claude call, never blocking | tags only / Claude call required | MVP | two-way | Y |
| D34 | Ordering defaults (spec C3.4); working ahead appends units, no enrichment mode | fixed sequence / enrichment mode | MVP | two-way | Y |
| D35 | New material at ceiling+1; untested ≤ceiling words appear as quick checks | reteach all / ceiling only | MVP | two-way | Y |
| D36 | Skill mix config per learner type × plan stage; stage by completed lessons; production scales with placement production ratio | old phase ratio table / fixed mix | MVP | two-way | Y |
| D37 | Phase clock runs during absence; unfinished units carry over; pause = slot | pause after 7+ days / extend end date | MVP + slot | two-way | Y |
| D38 | Plan reveal order (spec C3.8) | score-first reveal | MVP | two-way | Y |
| D39 | Adjust plan = 3 reasons, never touches completed work; "start fresh" dropped | old 4 reasons / free-form Claude replan | MVP | two-way | Y |
| D40 | Next phase starts at first open after end date; current goals and band, no re-placement | auto-start on date / re-placement | MVP | two-way | Y |
| D41 | Plan, plan unit and append-only plan event records (spec C3.11) | single plan blob | MVP | one-way (schema, F) | Y |
| D42 | 3 lessons per unit; arc introduce / extend / use; `lessons_per_unit` config | variable by item count / 1 per unit | MVP | two-way | Y |
| D43 | 10-12 min lesson; 4-6 new items in chunks of 3 with cumulative retrieval; ≥5 exposures per item | old 8 blocks, 15-25 min | MVP | two-way | Y |
| D44 | One item set per lesson; all activities recycle it; only reviews and quick checks come from outside | per-block vocab (old engine) | MVP | two-way | Y |
| D45 | Activity catalogue replaces 7 block types; conjugation folds into pattern; unknown type = build error | keep 7 types | MVP | two-way | Y |
| D46 | Lesson production = tap-to-build word bank, local grading, typed optional; no Claude grading in lessons (narrows Q9) | typed + fuzzy / Claude hybrid per lesson | MVP | two-way | Y |
| D47 | One romanization standard, Arabizi digit set, stored as data + validator (closes Q7) | academic diacritics / plain Latin | MVP | one-way (content, DR) | Y |
| D48 | Scaffold ladder by `script_stage` 0/1/2; Settings harakaat toggle overrides display only | romanization always on / learner-type ladder | MVP | two-way | Y |
| D49 | Quick checks 1-2 per lesson in warm-up; miss reclassifies word as new material | separate screen / drop | MVP | two-way | Y |
| D50 | Deterministic allocator maps skill mix to activity counts; production weight 0 renders recall as recognition | ratios inside the generation prompt | MVP | two-way | Y |
| D51 | Wrap-up contents; no mid-lesson difficulty override (feedback goes to Adjust plan) | mid-lesson override | MVP | two-way | Y |
| D52 | Lesson record + append-only answer rows with `response_ms` (spec C4.11) | single lesson blob | MVP | one-way (schema, F) | Y |
| D53 | Content pre-generated, shared, versioned; zero runtime Claude on lesson/quiz path. Closes Q10 | per-learner runtime / runtime with cache | MVP | one-way (content model, F) | Y |
| D54 | Item sets declared in catalogue: 6 ids, 4 core + 2 extension; unit level does ceiling+1 (amends C4.3) | runtime selection / rows per learner type | MVP | one-way (catalogue, F) | Y |
| D55 | Authoring pipeline: catalogue → skeletons → assets → validators → native review → publish | generate-and-ship / sampled review | MVP | two-way | Y |
| D56 | Blocking validators: schema, dictionary-ref, known pool, romanization, dialect assertions, exposure, no `[UNVERIFIED]` | warn-only / sample checks | MVP | two-way | Y |
| D57 | Runtime Claude surface = checkpoint eval, goal mapping, Focused Study custom topic; fixed clusters pre-generated; each capped with a degrade path | keep P5/P6 runtime / tutor in MVP | MVP | two-way | Y |
| D58 | Model + token budget chosen server-side per task from an allowlist; client sends neither | client-chosen / one model | MVP | one-way (gateway, G) | Y |
| D59 | `ai_call` row per runtime call; per-user monthly budget; soft cap degrades, hard cap blocks. Closes Q9 | no metering / block only | MVP | one-way (schema, F) | Y |
| D60 | Server-side versioned prompt registry; one shared client with schema validation, `stop_reason`, bounded retry; rows store `model` + `prompt_version` | prompts in client | MVP | one-way (registry, G) | Y |
| D61 | Content ids + `content_version`; lesson pins the version seen; report-a-problem targets id + version; edits publish new versions | mutate in place / freeze per phase | MVP | one-way (content model, F) | Y |
| D62 | Remedial micro-drill library authored in the same pipeline, keyed to confusion ids; selection owned by C6 | runtime remedial generation / none | MVP | two-way | Y |
| D63 | New material rate-limited per calendar day (config, default 3 lessons); sessions uncapped; overflow runs consolidation mode (review + quiz, no new item set). Narrows D15 | hard session cap / no limit / adaptive re-weighting now | MVP | two-way | Y |
| D64 | First review interval falls on a later calendar day regardless of same-day activity; spacing measured in days, never sessions | sessions-based intervals | MVP | two-way | Y |
| D65 | Contextual recycling quota in generated dialogues and patterns, default 30% of non-item-set tokens, validator-enforced | review queue only | MVP | two-way | Y |
| D66 | Placement misses at or below ceiling seed a `known_gaps` queue, prioritised into first units, counted as new material | discard (D27 as written) | MVP | two-way | Y |
| D67 | Listening accuracy weights the listening skill-mix share and audio replay allowance, or is removed from C2.5 | leave unused | MVP | two-way | Y |
| D68 | Production support ladders: word bank → distractor pressure → typed, by item mastery and plan stage; Settings toggle forces typed on only | Settings toggle only | MVP | two-way | Y |
| D69 | Recorded risk R1: spoken goal measured by text checkpoints in MVP; speaking practice closes it | leave unstated | MVP | two-way | Y |
| D70 | Unit ordering: no two consecutive units share a primary category unless no alternative exists at the learner's band | no constraint | MVP | two-way | Y |
| D71 | Review overflow: when due volume exceeds the warm-up budget, order by overdue age then mastery weakness; warm-up stretches to a config ceiling; remainder carries forward, never dropped | truncate queue / unbounded warm-up | MVP | two-way | Y |
| D72 | Scheduler: Leitner ladder, binary outcome, config intervals, behind a swappable `next(state, outcome, today)` interface; `review_event` keeps `response_ms` so FSRS can be fitted later | SM-2 / FSRS / no scheduler | MVP | two-way (one-way on event rows, F) | Y |
| D73 | Mastery = box + `due_on` (strength) plus status (counting), never one number; read-time decay retired | keep 0-100 + decay | MVP | one-way (schema, F) | Y |
| D74 | Intervals 1/1/3/7/16/35/90 with ±10% deterministic jitter; correct = box +1; incorrect = box 1; second consecutive incorrect = box 0 + remedial check; max one advance per calendar day | lapse to box 0 / no jitter / unlimited daily advance | MVP | two-way (config) | Y |
| D75 | Graded events = warm-up review, quiz, quick check, My Vocab self-rating. Introduce steps, culture notes and contextual recycling are exposures only | count every appearance | MVP | two-way | Y |
| D76 | Store entry values per C6.4: placement correct and quick-check correct seed at box 3 `already_knew`; misses at or below ceiling become `status = gap`; new item sets enter box 1 on lesson completion | seed all tested items / seed at box 1 | MVP | two-way | Y |
| D77 | Review queue: 8 default, ceiling 14 on overflow, remainder carries; order by overdue age then lowest box then weakness flag; no more than 3 consecutive items from one unit | fixed size / truncate overflow | MVP | two-way | Y |
| D78 | Words learned = box ≥3 with a graded correct review at interval ≥3 days and `already_knew` false. B8 threshold = 80% cohort, 70% per tester, on first-attempt scheduled reviews of learned items | count all box ≥3 / count every stored item | MVP | two-way | Y |
| D79 | Production ladder by box: 0-2 word bank · 3-4 word bank with distractor pressure · 5+ typed | plan stage only / Settings only | MVP | two-way | Y |
| D80 | `script_stage` advance: ≥85% rolling accuracy over last 20 script items (+ ≥8 micro-steps for 0→1, + <20% romanization reveal taps for 1→2); demote below 60%; max one step per 7 days | learner choice / lesson-count gate | MVP | two-way | Y |
| D81 | Remedial drills declare their own trigger item and pattern ids; 3 matching lapses at box ≥2 within 14 days fires one drill, max 1 per session, no repeat within 14 days | confusion tags on dictionary rows (DR cost) / runtime generation | MVP | two-way | Y |
| D82 | One `learner_item` store for all sources including custom My Vocab rows (`dictionary_id` nullable); My Vocab self-rating moves boxes but `self_rated_only` items never count as words learned | separate vocab tables / self-rating ungraded | MVP | one-way (schema, F) | Y |
| D83 | Band auto-advances on 9 of last 10 quick checks correct, min 10 lessons apart; never auto-demotes (Adjust plan owns "too hard") | no auto change / symmetric auto up and down | MVP | two-way | Y |
| D84 | Return after 7+ days = extended warm-up, ceiling 20, no new item set; consolidation mode pulls due reviews, then lowest-box not-yet-due items, then last 3 units, logging exposures without advancing | new material as normal / stretch warm-up only | MVP | two-way | Y |

| D85 | Quiz is a pre-generated end-of-lesson retrieval check over the lesson's item set; client-built quizzes retired | client-built from blocks / runtime-generated | MVP | one-way (content model, F) | Y |
| D86 | Composition: 1 question per delivered item-set item, +1 pattern question when the lesson had a pattern, +2 unit recall questions at L3; range 4-7 enforced at build; not-yet-due unit recall items log an exposure on correct and lapse on incorrect | no cumulative questions / fixed 5-7 sliced at runtime | MVP | two-way | Y |
| D87 | Quiz types are a subset of the C4.4 catalogue; each item's quiz question uses a modality its lesson exposures did not, asserted by the C5.5 validator | free modality choice / separate quiz type catalogue | MVP | two-way | Y |
| D88 | Fresh item-set items are always word bank in the quiz (no box to read yet); unit recall questions read the C6.9 ladder | ladder by plan stage / typed for heritage | MVP | two-way | Y |
| D89 | Local binary grading, immediate per-question feedback with correct form and audio, no explanation prose | end-of-quiz feedback only / Claude explanations | MVP | two-way | Y |
| D90 | Results show the raw count and per-item states, per lesson only, never accumulated or averaged; threshold stays internal, default 0.7 on first attempts, and below it the item set enters box 0 and carries into the next warm-up above the ceiling | no threshold at all / 70% gate with lesson repeat / hide the count entirely | MVP | two-way (config) | Y |
| D91 | Unscored practice-again on missed items from the results screen; the scored second chance is tomorrow's warm-up; no full re-sit | full re-sit best attempt / re-sit with first attempt counting / no retake at all | MVP | two-way | Y |
| D92 | No quiz table: questions are lesson activities, answers are answer rows with `origin = quiz`; lesson record gains `quiz_score` and `quiz_outcome`; `lesson_progress.quiz_result` retired | separate quiz and quiz_result tables | MVP | one-way (schema, F) | Y |

| D93 | Anchor: CP1 and CP2 fire at the earlier of day 30/60 or lesson 30/60; CP3 fires at phase end only, first open on or after day 90; turn budgets 6/10/14, minutes are display copy. Closes Q12, C1.7 and C3.12 | days only / lessons only / CP3 early-triggerable like CP1 and CP2 | MVP | two-way (config) | Y |
| D94 | Server-built content pack (covered units, stored items, band, `script_stage`, goals, allowlist, previous work-on list) plus an authored scenario catalogue keyed to marker and goal tags; prompt caps unknown words at 10% | free scenario choice by Claude / client-assembled context | MVP | one-way (catalogue, J) | Y |
| D95 | Typed free text, no word bank and no local grading in the conversation; turn rows written before render so resume works; "I'm stuck" costs a turn; 3 stalls trigger the soft landing; audio on tap per turn | word bank scaffolding / single-shot prompt with no resume | MVP | two-way | Y |
| D96 | Dimensions accuracy + vocabulary range (CP1), + spontaneity + elaboration (CP2), all four (CP3); bands emerging / developing / conversational / fluent; elaboration replaces harvest's speed, latency stored unscored | keep speed as scored (harvest §3b) / single overall band | MVP | two-way | Y |
| D97 | Debrief order fixed (what worked, 2-3 to work on, band, path to next band), compared to the previous from CP2, stored permanently, rendered in Progress | free-form narrative / band only | MVP | two-way | Y |
| D98 | Checkpoints log exposures and move no boxes; evaluator returns item ids used correctly and observed confusion ids, stored on the debrief row only and NOT wired into the C6.10 remedial counter in MVP. Closes C6.14 | feed confusions into the remedial counter (proposed, withdrawn: at most 1 lapse per 14-day window, can never fire a drill alone) / full box movement on checkpoint evidence | MVP | two-way | Y |
| D99 | Caps per checkpoint (max turns, max tokens per turn, 1 eval call); turn failure retries once then pauses and resumes; eval failure or hard cap renders a reduced debrief with bands withheld; no scripted fallback checkpoint | pre-generated fallback checkpoint per marker / abandon and re-offer | MVP | two-way (config) | Y |
| D100 | `checkpoint`, `checkpoint_turn` (append-only) and `checkpoint_debrief` records; exposures write `review_event` with `origin = checkpoint`; harvest `checkpoints` table retired | single checkpoint blob | MVP | one-way (schema, F) | Y |

| D101 | Audio is a pre-generated asset of the C5.4 pipeline, not a runtime service; exactly 1 runtime TTS surface (checkpoint turns) | synthesise-on-demand and cache (old build) / no runtime audio at all | MVP | one-way (content model, F) | Y |
| D102 | Content-addressed clips: key = hash of `tts_text` + provider + model + voice. Text edit creates a new key and clip; old clips survive for pinned versions. No clip generated twice | key by content id / key by raw display text | MVP | one-way (asset model, F) | Y |
| D103 | TTS reads a vocalised string, never bare script. Dictionary gains `arabic_vocalised` in DR; authored lines carry a vocalised field. `tts_text` stored separately from display text, so dialect pronunciation is spelled into the input. Also the source for the C4.8 harakaat rung | strip and hope / romanization-to-phoneme mapping | MVP | one-way (column, DR + F) | Y |
| D104 | Voice is track-scoped, server-side from an allowlist (mirrors C5.8). MVP = 2 voices, A/B for dialogue roles, A for all word clips. Every clip stores provider, model, voice; a voice change regenerates under a new key | 1 voice everywhere / learner-selectable voice | MVP | two-way | Y |
| D105 | `human_recorded` flag plus the same path contract: a recorded file replaces a generated clip as a content edit, no code change. MVP ships TTS only; §J spike decides if the slot gets used | TTS only with no slot / record a core set now | MVP + slot | two-way | Y |
| D106 | Supabase Storage, immutable public path from the key; client caches in Cache Storage via the service worker plus an in-memory session map; lesson start prefetches the full lesson + quiz manifest (extends D19). In-memory-only Map retired | IndexedDB blobs / no persistent cache (old build) | MVP | two-way | Y |
| D107 | Tap only, never autoplay; unlimited replays everywhere; Slow control sets `playbackRate` 0.75 on the cached clip at zero generation cost; `replay_count` and `slow_used` on answer rows | capped replays on graded items / separate slow clips | MVP | two-way | Y |
| D108 | D67 discharged: listening accuracy sets a default, not a limit. Below a config threshold, Slow is pre-selected and a second play offered on graded listening items. No learner capped | drop the replay clause from C2.5 / hard 2-play cap on desirable-difficulty grounds | MVP | two-way (config) | Y |
| D109 | Web Speech `ar-SA` retired outright. On failure: retry, then the item continues silently and logs it; a graded listening item with no audio swaps to its script counterpart rather than being scored blind | keep as last-resort fallback / keep for review items only, never Introduce | MVP | two-way | Y |
| D110 | Runtime TTS = checkpoint turns only, on tap, 1 synthesis per turn, cached under the same key, capped per checkpoint, degrade = text-only turn. Focused Study custom topics and custom My Vocab rows get word clips only where the Arabic resolves to a dictionary row | synthesise custom content too / no checkpoint audio | MVP | two-way | Y |
| D111 | Runtime synthesis writes an `ai_call` row with `task = tts`, characters in place of tokens; authoring synthesis is one-off and unattributed; B8 Affordable reads both | separate `tts_call` table / no metering | MVP | one-way (schema, F) | Y |
| D112 | Audio validators blocking at publish (extends C5.5): asset resolves with `status = reviewed`, duration bounds, no zero-byte clips, voice inside the track allowlist. Native review listens to 100% of word clips at `level` 1-2 plus every dialogue line, sampled above | spot-check only / no audio gate | MVP | two-way | Y |

| D113 | Track is data: one row + content package + config overlay; MVP 1 active track `palestinian`, no picker | enum in code / defer the track key until a 2nd track exists | MVP + slot | one-way (scoping, F) | Y |
| D114 | Dictionary track-scoped by row (`track_id` NOT NULL); cross-track `concept_id` named as an extension point, not added empty | global lemma table + `dictionary_track` join / separate table per track / no track column | MVP | one-way (schema, F) | Y |
| D115 | `track_id` stored on roots, derived on children; content ids carry a track prefix; C5.5 asserts no cross-track references | `track_id` on every row / derive everywhere from `dictionary_id` | MVP | one-way (schema, F) | Y |
| D116 | `learner_track` enrolment row supports concurrent tracks (B6 "alongside"); MVP writes 1; `profiles.active_track_id` = session focus | scalar on profile only, enrolment derived from plan rows | MVP + slot | two-way | Y |
| D117 | Learner-global: `script_stage`, streak, daily new-material limit, AI budget. Track-scoped: ceiling, listening accuracy, production ratio, plan, store, words learned, My Vocab. Script evidence pools across tracks | all track-scoped / all learner-global | MVP | two-way | Y |
| D118 | Config = versioned JSON in the repo, resolved once (base → track → learner type → Settings) into a frozen object; no feature branches on track or learner type, CI grep gate; `runtime_flag` allowlist for operational overrides only | Supabase config table with live edits / constants in code (old build) | MVP | one-way (resolver, E) | Y |
| D119 | Frozen vs hot config keys; frozen pinned at plan start and changed only at a phase boundary; every record pins the resolved version | one global version, latest always wins | MVP | two-way | Y |
| D120 | Adding a track is a content operation with zero code changes; the 6-step acceptance test is the §I roadmap proof | prove it only when the 2nd track is built | slot | two-way | Y |
| D121 | DR gains `track_id` backfill and `dialect_tag` → `register` enum; C2.2 filters on register | leave `dialect_tag` free text / tag tracks per row | MVP | one-way (column, DR) | Y |

| D122 | §C11 owns shape, content source, store interaction and records for Focused Study, My Vocabulary and Alphabet; §D owns their screens; all three share one store, one config and one content pipeline | specify them inside §D / leave implicit | MVP | two-way | Y |
| D123 | Focused Study custom topic is a mapping call only; sessions assembled deterministically from published assets; degrade = nearest fixed cluster | runtime generation of custom session content | MVP | two-way | Y |
| D124 | My Vocab rows are `learner_item` rows; custom rows `self_rated_only` and never counted as learned; graded evidence upgrades them; community pool = stored opt-in flag only | separate vocab table (harvest `personal_vocab`) / self-rating ungraded | MVP + slot | one-way (schema, F) | Y |
| D125 | Alphabet practice counts toward the C6.9 micro-step requirement but not toward the rolling accuracy window that advances `script_stage` | count toward both / count toward neither | MVP | two-way | Y |
| D126 | Chat 11 retro-audit fixes A1-A7 applied to approved sections | leave approved sections closed and carry the contradictions into the build | MVP | two-way | Y |

| D127 | `level` = global ordering by how early a learner needs the item to form and survive Palestinian sentences. Level 1 has 3 required strands: sentence machinery, core concrete lexis, obligatory formulas. Verb-heavy. Not frequency, not difficulty, not teaching order | frequency-derived / difficulty-derived / within-category bands | MVP | one-way (column, DR) | Y |
| D128 | `pos` enum gains `formula` (whole conversational turn) and `frame` (productive pattern); `Phrase` and `Expression` retired. Token count is irrelevant: مبروك is a formula, not a one-word noun | keep Phrase/Expression / split by token count | MVP | one-way (column, DR) | Y |
| D129 | `dialect_tag` splits into 2 orthogonal columns: `register` (neutral/slang/formal) and `form_origin` (dialect / msa_shared_dialect_pron / msa_identical / regional_variant). The single column forced a false choice for rows that are both dialect-distinctive and slang. Amends D121 | single `register` enum as D121 assumed / leave free text | MVP | one-way (columns, DR) | Y |
| D130 | `formula_pair` table (many-to-many, `addressee`, `obligatory`). Call-and-response is structural in Palestinian Arabic; teaching مبروك without الله يبارك فيك is not teaching. Pairs share a level, are never distractors for each other, and bind a C5.5 blocking validator | `response_to` column / leave implicit in the English gloss / defer | MVP | one-way (table, F) | Y |
| D131 | Third-party essentials lists are a manual cross-check by Marwan as a subscriber, never an automated diff or ingestion. Public metadata only (learnlevantine: 667 essentials of 2,785, 40% verbs, 24% essential share) used as sanity bounds, never quotas | scrape under subscriber credentials / import the list / ignore external signal entirely | MVP | two-way | Y |
| D132 | Learned counts split into words learned (lexemes) and phrases learned (`formula`, `frame`). Dictionary and `learner_item` store stay unified; only the counter splits. A phrase contributes nothing to words learned: mastering a chunk can be contextual recognition, so its parts are never credited. B8 reads words learned only; phrases learned is progress feedback, not a metric (revised chat 12 from the 60% lexeme share) | one combined figure (D78 as written) / combined with a lexeme share floor / separate phrases into their own table and UX | MVP | two-way | Y |
| D133 | `formula` and `frame` rows declare their constituent dictionary ids. C5.5 resolves the row as an item AND checks constituents against the pool separately, so a level 1 frame cannot smuggle an above-level lexeme. Declaration is a validator input only, never an evidence path into mastery | resolve phrases whole / tokenise at validation time without stored constituents | MVP | one-way (column, DR) | Y |
| D134 | P10 becomes 3 regimes: graded positions hard-gated at or below unit level; generated input prose allows a capped above-level share, default 10% matching D94; learner-directed surfaces ungated. The unit level is already learner ceiling+1 (C4.3), so the item set is the controlled +1 and the cap is the incidental one. Amends `principles.md` P10 | keep the single hard ceiling / raise the pool to unit level+1 | MVP | two-way (config) | Y |
| D135 | C11.2 Focused Study and C11.3 My Vocabulary are exempt from the level ceiling. Learner-chosen study is not the system choosing what the learner meets. All other C5.5 validators still apply | inherit the C5.5 gate as written / exempt My Vocabulary only | MVP | two-way | Y |
| D136 | New repo `mharb92/hoopoe` @ `main` is the rebuild base, not a branch. `mharb92/arabic-app` @ `modular-rebuild-5` @ `7dc0b14` is read-only harvest, archived after DR promotion. No other ref is ever cited | branch off `modular-rebuild-5` / keep building in place | MVP | one-way | Y |
| D137 | New Supabase project `hoopoe` (`pniwgnjljpkiimssortp`, free org, West EU Ireland, expose-new-tables off, auto-RLS on) is the build target. Only `dictionary` migrates: 2,728 rows, ids preserved exactly, never renumbered. The 4 profiles and 20 `personal_vocab` rows are dropped; `personal_vocab` re-imported once §F defines its shape. Old project `xkhulybdrxdzakarivvi` stays live and read-only until DR promotes, then pauses. Supabase GitHub integration stays disconnected: it deploys schema on push and cloud sessions push to the repo | reuse the old project / migrate every table / connect the GitHub integration | MVP | one-way (data) | Y |
| D138 | Brand is data, not code: one `BRAND` config object, neutral storage and cache key prefixes, no brand string in table or column names. Name stays Hoopoe for now but is unsettled | hardcode the name / defer until launch | MVP | two-way | Y |
| D139 | Dictionary migrated to `pniwgnjljpkiimssortp` over PostgREST from a Claude Code cloud session, sourced from the committed `tools/dr/snapshots/dict-preDR.json` snapshot rather than a live re-pull, in 14 batches of 200 with `on_conflict=id`. 2,728 rows, ids preserved, `id||arabic` sha256 `bd2f7967…` identical on both sides, sequence resynced with `setval`. Gate held: a mismatch would have stopped the run | browser CSV export/import via the Table Editor / Management API SQL endpoint with a personal access token | MVP | one-way (data) | Y |
| D140 | DR batch runner specced (`docs/dr/dr-runner-spec.md`): Node ESM, zero dependencies, run from a `hoopoe-dr` cloud session; one model seam with transport as config (`anthropic-direct` pilot, `edge-function` full loop); seeded batch plan reproducible from the seed alone; resume on `(run_id, dictionary_id)`; **no code path that writes `dictionary`**, so promotion stays a hand-run SQL step. 7 resolutions where the frozen docs could not be executed as written: rule-fix folds into the single staging row (`payload.rule_fix`); row `status` stays coarse with per-field detail in `payload.routing`, so the frozen promotion SQL is unchanged; grouped batches judged in sliding slices with the full set as context; batch size is config; rows failing validation are left unstaged and logged, never patched to pass; hard stop conditions; raw output stays in `payload`, never in git. Pilot gate P1-P8 replaces F1-F7 for the first batch, since F1, F2, F6 and F7 are global floors. Held rate and batch size are decided at the pilot with the measurement in hand | runner enforces F1-F7 as its pilot gate / per-field `status` column / promotion inside the runner | MVP | two-way | Y |
| D141 | DR pass scoped for the MVP (`docs/dr/dr-scoped-pass.md`, `docs/dr/dr-prompt-scoped.md`). One unattended machine pass collects `level`, `romanization`, `arabic_vocalised`, `pos`, `register`, `form_origin`, flagged `corrections` and `native_check`. Deferred: `pair`, `constituents`, corrections triage, adjudication worksheet, F1-F7, gap cross-check — each attached to a named gate, not a list. New `dictionary.review_confidence` 1-3, the minimum across critical fields, plus `native_verified` as a separate boolean; MVP content selection filters `review_confidence = 3` and re-review upgrades rows in place without republishing. Principle: anything the model can emit while already reading the row is near-free, every human queue is expensive, so collect wide and defer the queues. Amends C2.2 and C5.5; new risk R7 | run the full pass as specced / cut by field and re-collect later / keep the confidence signal in staging only | MVP | two-way | Y |
| D142 | One dark theme, fixed. No `prefers-color-scheme`, no toggle; token architecture unchanged, so a light theme is a values file later | light only / both with a toggle | MVP | two-way | Y |
| D143 | Tokens in three layers, primitive → semantic → component, consumed only at the semantic layer. Warm near-black surfaces and off-white text, never `#000` or `#FFF` (halation on fine marks). `accent-fill` vs `accent-on-dark` split. Elevation is lighter surfaces, never shadows. Lora / DM Sans / Noto Naskh; Arabic gets its own size and line-height tokens. Build fails on `style=`, a duplicate selector, or a colour literal outside the primitive layer. Target 393×852 | single-layer token set / component-level colour literals | MVP | one-way (token architecture) | Y |
| D144 | One component renders Arabic on every surface, owning `dir`, `lang`, font, the C4.8 rung, harakaat override, audio affordance, report target and accessible name. Takes a dictionary id or line ref; a caller cannot pass a display flag | per-screen rendering with shared CSS / display flags passed by callers | MVP | one-way (component seam) | Y |
| D145 | Reveal is per occurrence and resets at the activity boundary; the hidden rung is a labelled control, not an invisible hit area. A session-long reveal would let `script_stage` advance on scaffolded evidence (P6) | session-long reveal / per-screen reveal | MVP | two-way | Y |
| D146 | Arabic and Latin are never concatenated into one string: separate elements, own `dir` and `lang`, bidi-isolated, stacked not inlined. Digits Latin per D47 | single string with bidi marks / inline spans | MVP | two-way | Y |
| D147 | One Arabic input. Fixed RTL for Arabic-only answers, `dir="auto"` where either language is legitimate. Autocorrect, autocapitalise, spellcheck **and smart punctuation** off: iOS substitutes a typographic apostrophe and the romanization standard uses `'`, so every glottal answer would fail the C4.6 check invisibly | rely on `dir="auto"` everywhere / leave iOS defaults | MVP | two-way | Y |
| D148 | Three tabs: Home, Progress, Settings. Session, checkpoint and entry stages are full screen with chrome hidden. Focused Study, My Vocabulary and Alphabet live in Home's card grid (C1.2 config order). One persistent bottom element per screen. No FABs, no splash, no coming-soon | four or five tabs with side surfaces promoted / drawer navigation | MVP | two-way | Y |
| D149 | The app opens to Home, never into a session. The primary button's label carries the resolved state. Install card appears once in Safari before sign-in | open into the resolved next activity | MVP | two-way | Y |
| D150 | Stage resolution from server records: auth → onboarding → placement (resumed) → plan reveal → Home. Back inside a tab goes up one level; back inside a session opens save-and-exit confirm. Deep links in MVP: invite and sign-in only | device-state resolution / a stored stage pointer | MVP | one-way (matches C1.5) | Y |
| D151 | Two Arabic faces, one component picks. Amiri display (wordmark, titles, 28px+); Noto Naskh learning (everything decoded, including the alphabet screen). Never both for one word on one screen. Discretionary and contextual ligatures off on the learning face; lam-alef is obligatory so it gets a named micro-step (D26). Learning face preloaded, display lazy | one face throughout / a display face for all Arabic | MVP | two-way | Y |
| D152 | One accent-filled element on Home, the primary card; label carries the C1.2 state, sub-line the position. At the `new_lessons_per_day` cap the label becomes Consolidate today: no lock, no warning colour, no counter. A due checkpoint puts Not now on the card as a visible secondary | lock the card at the cap / show a remaining-lessons counter | MVP | two-way | Y |
| D153 | Home shows four C6.12 figures: streak, words learned, phrases learned, items due. No quiz figures, percentages or target bars. Secondary cards carry no badges, counts or new-dots. After 7+ days away, one line plus a sub-line, no modal | a richer dashboard / a re-entry modal | MVP | two-way | Y |
| D154 (revised on review) | One activity frame: progress bar and save-and-exit, prompt line, stimulus block, response zone. An activity screen is a stimulus block plus **zero or more** response widgets, because C4.2 counts screens and item-batched types carry several items each. Widgets: choice, build, type, reveal. Single-item screens use the bottom feedback sheet; item-batched screens resolve inline with one Continue. An unmapped activity type fails the lesson build | one screen per item / a separate engine per activity type | MVP | one-way (frame contract) | Y |
| D155 | Explicit Continue always, nothing auto-advances. Correct and incorrect both render the correct form with audio on tap. Incorrect is "needs another look", never a verdict word, matching the results screen. Motion is a token-duration fade | auto-advance on correct / verdict words | MVP | two-way | Y |
| D156 | The quiz is the same frame with a different segment label, not a second engine. Segment transitions are one line, never an interstitial. Results: count once, every item with state, correct form and audio, needs-another-look items named as tomorrow's warm-up, then practice again and continue. No pass, fail or percentage | a distinct quiz UI / an interstitial between segments | MVP | two-way | Y |
| D157 | Stimulus zone scrolls; progress bar and feedback sheet pinned. The progress bar spans the whole session including the quiz and never resets at the segment boundary. Save-and-exit confirms by naming the return point; a mid-quiz exit preserves answered questions | full-screen scroll with nothing pinned / per-segment progress bars | MVP | two-way | Y |
| D158 | Placement is the activity frame with feedback suppressed by config, not a second engine. Persistent "I don't know", no feedback sheet at all (C2.8). Production self-rating is a second screen after a correct Listen. Band intros one line, no emoji. Plan reveal is one scrollable screen in the C3.8 order; reassignment is a better starting point with one-tap undo, never a level change | a bespoke placement UI / inline self-rating | MVP | two-way | Y |
| D159 | The checkpoint is a transcript surface, deliberately unlike a lesson. No item progress bar; a thin unlabelled bar tracks turns used. Named character bubbles, audio on tap, report-a-problem per turn. Learner field is free text with `dir="auto"`, no word bank, no grading affordance. "I'm stuck" states its cost before use. Debrief in C8.6 order, bands as words on a four-step scale. A reduced debrief renders plainly, not as an error | the lesson frame reused / a turn counter shown as a number | MVP | two-way | Y |
| D160 | Chrome hides whenever a graded activity runs, anywhere in the app. Side surfaces keep the tab bar while browsing; the moment an item starts, the D154 frame takes over. Focused Study is the 6 clusters plus a custom topic; an unmapped topic offers the clusters in plain copy, never an error | keep chrome on side-surface practice / error on an unmapped topic | MVP | two-way | Y |
| D161 | TSV import is a paste box first, file picker second. Either route lands on a preview table with per-row status; nothing is written until confirm. Custom rows with no match render silent with a one-line reason. One line on the surface says self-rated items don't count toward words learned | file picker only / write on import with undo | MVP | two-way | Y |
| D162 | Report-a-problem sits in the same position on every activity screen: an overflow control on the stimulus block, one tap plus an optional note, never blocking, confirming inline. Same component and record shape on every surface, so §H triage reads one table | per-surface report affordances / a Settings-level report entry | MVP | one-way (record shape) | Y |
| D163 | Audio is one button, replay always available. Below the C9.6 listening threshold, Slow is pre-selected and a second play offered on graded listening items, as a switchable default and never a cap (D108) | separate play and slow controls / a hard replay cap | MVP | two-way | Y |
| D164 (revised on review) | Progress is a history surface: no primary action, no items-due figure. Home's four figures deep-link by kind — words and phrases to My Vocabulary filtered to learned, items due starts a review, streak opens Progress | Progress as a detailed copy of Home's figures / all four linking to Progress | MVP | two-way | Y |
| D165 (revised on review) | Progress shows the two C6.12 counters over time, plan position, completed units as can-do statements, streak current and longest as counts, checkpoint history and the script rung. Cumulative only, never a rate, target or comparison. No streak calendar grid: a grid renders every missed day as a visible gap, breaking C1.4 by layout | a weekly rate or target / a streak calendar | MVP | two-way | Y |
| D166 | Mastery internals stay internal: no box numbers, intervals or accuracy percentages. The script rung is the exception and renders as words on a named ladder, never a number | expose box counts as a progress signal | MVP | two-way | Y |
| D167 | Progress counts, it does not list. Learned counters tap through to My Vocabulary rather than Progress growing a parallel list, which would give §H two report paths for the same rows | a read-only learned list on Progress | MVP | two-way | Y |
| D168 | Checkpoint history opens stored debriefs read-only in C8.6 order, with the CP2 comparison as stored and never recomputed (R6). Archived turns keep audio and the report control. A reduced debrief renders plainly in history too | recompute comparisons against the latest checkpoint | MVP | two-way | Y |
| D169 | Settings is a closed inventory: Adjust plan, harakaat toggle, typed answers toggle, audio Slow default, sign-out, app version. Absent, not greyed: export and deletion (§B6), notifications, a general report entry, learner type, TSV import | greyed placeholders for the §B6 items / a general report entry | MVP | two-way | Y |
| D170 | Settings exposes only display-only or already-eventful keys. Nothing in Settings can redefine what a learned word, band or quiz outcome means (R6). The typed-answers toggle takes effect at the next activity boundary, never mid-screen, with the resolved config version pinned per record | expose any hot key in Settings | MVP | one-way (R6 boundary) | Y |
| D171 | Adjust plan is a sub-screen. Too easy and too hard confirm with the effect in capability words, and the confirm copy states what is preserved. Change focus opens goal editing and unstarted-unit reorder in one screen | a direct two-tap band control | MVP | two-way | Y |
| D172 (revised) | Count-based ranking, tiers and titles are out of MVP; §B7 keeps excluding leaderboards, XP and badges permanently. What files to §B6 is a status-by-capability slot: standing earned by demonstrated capability, in can-do terms. A title at a word count is a badge under P17, and `learned` is config-defined so a mid-phase change would retroactively promote or demote (R6) | ship titles tied to words learned / amend P17 to permit non-competitive titles | roadmap | two-way | Y |
| D173 (revised) | Proficiency goals are shaped now, built later. Nullable and unpopulated in MVP: `unit.descriptor_ref` (framework + descriptor id) and `plan.target_descriptors`, distinct from C3.3 topical goals. C8.9 already stores per-dimension bands, so trajectory is derivable without new instrumentation. No CEFR level displayed, claimed or computed in MVP. The §B6 slot is a proficiency review reporting a descriptor **range**, gated behind speaking practice: R1 makes a text-only level claim unsound and B2 forbids a pass/fail verdict | CEFR levels on checkpoint debriefs in MVP / an internal Hoopoe-named level scale | MVP shape, roadmap feature | one-way (columns, F) | Y |
| D174 | §B6 gains two lines, status by capability and proficiency review; §I gains the matching proof obligation — show a descriptor set attaching to units and plans and a review computing from existing checkpoint records, with no migration on written records and no change to how `learned` is defined. Amends §B6 and adds to §I scope | file the slots without a proof obligation | roadmap | two-way | Y |
| D175 | One degrade vocabulary across all five paths (three C5.7 surfaces, audio, C5.9 caps). A degrade announces itself only when the learner's own request went unfulfilled or the task changed; otherwise silent and logged. Copy never names mechanism, provider, model or cost | one generic failure message for all paths | MVP | two-way | Y |
| D176 | No toasts anywhere. Every confirmation, degrade line and error renders in place and stays until the learner moves on. Generalises D162 rather than leaving it an exception | toasts for low-stakes confirmations only | MVP | two-way | Y |
| D177 | Three loading tiers by elapsed time: nothing under 300ms, a layout-shaped skeleton 300ms-3s, skeleton plus progress copy and an out beyond 3s. No full-screen blocking spinner at any tier; chrome never changes because something is loading. Arabic skeletons use the Arabic line-height tokens | spinner at all tiers | MVP | two-way | Y |
| D178 (revised on review) | Every empty state carries copy, and a next action wherever an action exists on that screen. Where the only way to fill the surface is to use the app normally, the copy states what will appear and offers nothing, because a button back to Home is a dead end dressed as help. Zero items due is not an empty state; it is the finished state (P17) | require an action on every empty state / treat zero due as empty with a prompt to practise more | MVP | two-way | Y |
| D179 | Errors render at the smallest scope that owns the failure: item, then screen, never app (C1.6). Copy names a next action, never a code or a technical cause. Where a content item is involved, the D162 control is in its usual position | a shared error screen with a reference id | MVP | two-way | Y |
| D180 | Offline is not a feature in MVP (§B6). Answers queue and sync on reconnect; a loaded session runs to the end of its loaded content; anything needing new content stops with copy and a retry. The connection indicator appears only when there is unsynced work or a connection was just needed, never as a standing readout, and unsynced work is stated as held safely | a permanent connectivity chip / partial offline content | MVP | two-way | Y |
| D181 | No state introduces a second persistent bottom element, changes chrome visibility, or moves the report control. Keeps states from accumulating chrome one exception at a time | leave it implicit in D148 and D160 | MVP | two-way | Y |
| D182 | Second person, present tense, sentence case. The app never says "we" (the §B6 tutor will need that voice). No exclamation marks, emoji or praise adjectives; capability over effort (P17). One line where possible, two sentences maximum. Numbers as digits. A missed day is never framed as a lapse | a warm encouraging register | MVP | two-way | Y |
| D183 | Learner-facing glossary: session, lesson, unit, plan, checkpoint, review, word, phrase, streak, alphabet. Banned in copy: box, interval, scheduler, item, item set, mastery, level, threshold, accuracy, confidence, ceiling, band, track, config, learner type, persona, placement band, `already_knew`, script stage. Band *values* render as words; the rung renders as a name | allow internal terms where accurate | MVP | two-way | Y |
| D184 | Fixed names: Hoopoe, wordmark الهدهد, Home, Progress, Settings, Focused Study, My Vocabulary, Alphabet. Exact strings everywhere, no abbreviations, no icon-only tab labels | rename Focused Study to something warmer | MVP | two-way | Y |
| D185 (revised against the merged map) | Romanization renders in DM Sans, never Lora, italic, letter-spaced, small-caps or monospace. Lowercase and restricted to `a-z`, `2 3 6 7 9`, `'`, `-` per romanization-map rule 4, so any other character is a data signal. Apostrophe is U+0027 and never passes a typographic quote filter. A romanized token never breaks and never truncates — it wraps at whitespace only and a narrow container grows or reflows — because the digraph breaker, sun-letter assimilation and the five-long-vowel contrast all mean a break or ellipsis changes the word. Lists never sort on the romanization field: word-initial `2` puts glottal-initial words ahead of the alphabet. Never appears without its Arabic (C4.8) | monospace for digit stability / soft-wrap at hyphens / sort on romanization | MVP | two-way | Y |
| D186 (revised on review) | Specimen copy set for the D.10 state inventory, establishing the register: one line each, second person, no mechanism, no apology. The two that could read as failures (reduced debrief, zero due) do not | apologising in error copy | MVP | two-way | Y |
| D187 | A row whose `form_origin` is `regional_variant` renders a short variety label beside its romanization, taken from the row and never inferred at display time. The target variety writes `2` for hamza and ق alike; rural and Hebron rows write `q`, `k` or `g`, and `g` is also a loanword letter | filter regional variants out of MVP content / normalise every row to urban pronunciation | MVP | two-way | Y |
| D188 | 44×44pt minimum targets, hit area allowed to exceed the visible mark. Named as at-risk: the reveal rung, the report control, the audio button, word bank tiles. Adjacent targets carry a minimum gap, since item-batched screens stack several response controls | scale targets with dynamic type | MVP | two-way | Y |
| D189 | 4.5:1 body, 3:1 large text and meaningful control boundaries, measured against the **lightest** surface token in the elevation set, not the base, because elevation is lighter surfaces. CI computes ratios from the token files and fails the build. Gold never carries text meaning alone; no state distinguished by colour alone | check contrast by eye at design time / measure against the base surface | MVP | one-way (CI gate) | Y |
| D190 | Nothing conveys information by motion alone. Under `prefers-reduced-motion` the fade resolves instantly and the progress bar steps; no state becomes unreachable, no copy changes. A token swap, not a second code path | a shortened animation under reduced motion | MVP | two-way | Y |
| D191 | Content scales across the full dynamic-type range, chrome scales to a cap. Pinned elements together never exceed a fixed share of viewport; the stimulus zone absorbs the rest by scrolling. The feedback sheet grows then scrolls internally rather than pushing Continue off screen. Arabic scales on its own tokens with the line-height **ratio** preserved, or harakaat clip. No supported size drops a control or truncates a romanized token | cap Arabic scaling to protect the layout | MVP | two-way | Y |
| D192 | The D144 component owns the accessible name, and the name is the vocalised string (`arabic_vocalised`, the same source as TTS). Romanization is hidden from assistive tech entirely — a Latin voice reads `2awlaad` as a number followed by nonsense. The English gloss carries meaning in the accessible tree. The reveal rung announces its state; the audio control names what it plays | expose romanization as a pronunciation hint on the Arabic element | MVP | two-way | Y |
| D193 | Every audio affordance is tap-only (P5); the one hard dependency is a graded listening item, and C9.7's swap to the script counterpart is the accommodation. A config key sets that swap as a standing preference: defined, unpopulated, no Settings control in MVP | a Settings toggle in MVP / treat it as a §B6 concern and say nothing | MVP shape | one-way (key, F) | Y |
| D194 | Four self-hosted families, never a third-party CDN. Subset by codepoint retaining layout tables: Arabic shaping runs on GSUB and GPOS, so a glyph subset gives disconnected letters, a broken lam-alef and mispositioned harakaat. `init`, `medi`, `fina`, `rlig`, `mark` retained explicitly with a rendering test on every font change. The learning face subsets to the whole Arabic block, not to current content, and drops Latin coverage. Learning face preloaded with `font-display: block` (a fallback may misposition harakaat, so swapping would show a wrong rendering then correct it); display face lazy with `swap` | subset per page by content / glyph subsetting / CDN fonts | MVP | one-way (build pipeline) | Y |
| D195 | Mascot is SVG source, transparent background, legible on dark and light so a later light theme is not a redraw. Rasters generated from the SVG. Decorative everywhere: hidden from assistive tech, never the sole carrier of meaning, never the only content in an empty state, no animation. Closed placement list: onboarding, empty states, wrap-up, app icon. Not Home | a mascot presence on Home for personality | MVP | two-way | Y |
| D196 | Icons at 192, 512, maskable 512 with the 40% safe zone, 180 Apple touch. No text in any icon. `background_color` and `theme-color` both the base surface value, status bar style dark, so launch never flashes white. No badge icon in MVP, since D169 removed notifications and C1.6 refuses affordances for things that don't exist | ship a badge icon as cheap insurance | MVP | two-way | Y |
| D197 | Source assets and built outputs both committed in separate folders; outputs are not generated in CI, because the workflow is browser-only and a CI-only step cannot be debugged. Regeneration runs in a cloud session and lands as a commit with the command recorded beside the output. All four families are OFL: license shipped, original names preserved | a CI font pipeline / CDN fonts | MVP | two-way | Y |
| D198 | Script rungs are named for what the learner reads unaided, not for what is hidden: `Reading with full support` · `Reading with harakaat` · `Reading unaided`. Rendered on Progress only, never inside an activity, where a stage label would read as a grade | name the rungs for what is withdrawn | MVP | two-way | Y |
| D199 | Correct renders the correct form with audio plus a non-colour mark and the label `Correct`; incorrect uses the same structure with `Needs another look`. D182 bans praise, not naming the outcome: `Correct` is a fact about the answer, `Great` is a judgement about the learner. Without a label the state rests on a mark and a colour, which D189 forbids | no label at all on correct | MVP | two-way | Y |
| D200 | Onboarding is three screens, one question each, no progress indicator: a three-of-three counter on a one-minute flow measures nothing. Name, learner type, goals. No back out, no skip | one scrolling form | MVP | two-way | Y |
| D201 | The learner-type question renders C1.3's wording as two full-width choices with no headline restating it. The options describe what happens when family speak, not what the learner is; heritage, beginner and intermediate never appear. Nothing promises the answer is final | a third option for confident speakers | MVP | two-way | Y |
| D202 | Goals are multi-select tags plus one optional free-text field on one screen, at least one tag required. The C3.3 mapping call runs after the learner continues, never as a blocking wait. Free text is `dir="auto"`. When the surface is degraded the field is not offered and nothing explains why (D175) | free text on its own screen / block on the mapping call | MVP | two-way | Y |
| D203 | Onboarding hands straight to placement with no interstitial; the last control says what happens next, and D158's band intros carry the framing from there | a short explainer before placement | MVP | two-way | Y |
| D204 | Wrap-up is a screen after results, not the results screen: results closes the quiz, wrap-up closes the day. Merging them puts a streak figure next to an item just answered wrong | extend results with the C4.10 figures | MVP | two-way | Y |
| D205 | Wrap-up shows items practised, items added to vocabulary and streak, plus one primary action resolving per C1.2. No quiz count, percentage or per-item states. At the daily cap the action carries D152's consolidation copy, identically worded | a fourth figure for words learned | MVP | two-way | Y |
| D206 | The streak increments on wrap-up with no celebration, no animation and no mention of days missed. A broken streak shows the new count starting again with no acknowledgement that it broke | mark a restarted streak to avoid confusion | MVP | two-way | Y |
| D207 | The alphabet screen is a grid in Arabic alphabetical order, each letter opening a detail view. Letters already met carry a mark; unmet letters render identically, no lock, no coming-soon. No tabs, no separate phonics section | order by teaching sequence / split phonics into a tab | MVP | two-way | Y |
| D208 | A letter detail shows the four contextual forms, the sound, audio on tap and practice. The isolated form is not privileged: a learner who only sees isolated forms cannot read a word. Lam-alef gets its own entry rather than sitting inside lam or alef | isolated forms only | MVP | two-way | Y |
| D210 | Vowel length is a per-row model judgement inside the produced `romanization` value: decided from the word, never carried over from the existing string, never rule-fixed and never a `corrections` entry. `rulefix.mjs` may not map `oo`/`uu`/`ee`/`ii` onto each other. New pilot check P10 (vowel-length flip rate) detects source pass-through. Native spot check samples at least 20 long-vowel rows of the 40. Not a correction because a flagged romanization correction caps `review_confidence` at 2 and most rows carry a long vowel, which would collapse the pool MVP content selection filters on. R7 extended: Arabic script does not disambiguate vowel length either, so `arabic_vocalised` cannot cross-check it | defer to the second pass / human worksheet per row / route through `corrections` | MVP | one-way at promotion | Y |
| D209 | Alphabet practice runs in the D154 frame with chrome hidden, writes `review_event` with `origin = alphabet`, counts toward the C6.9 micro-step requirement and not toward the rolling accuracy window, and says nothing about either. Always available, never gated by letters met | surface progress toward the 8 micro-steps | MVP | two-way | Y |
| D211 | The `test-driven-development` skill states test-first without exceptions. Deleted: the "Testing Strategies for Non-Developers" ladder (manual / light testing *(recommended)* / full TDD as a "future goal") and the "Recommended Approach" `Skip for` list, which contradicted the same file's "Always Trigger For". Also cut the two frontmatter clauses carrying the same thesis: "Progressive strictness for non-developers", which described `code-validation`'s mechanism and nothing in the file, and "Optional for simple projects, recommended for production", which sat in the description that decides whether the skill fires. A failing test comes before the fix for every feature, bug fix and refactor, UI styling included; the repo held no other written permission to ship untested code. Not a refactor entry: it changes behaviour, not shape. Merged to `main` as PR #4 | soften the core principle to match the ladder / keep the ladder and scope it to prototypes | process | two-way | Y |
| D212 | TypeScript everywhere; esbuild the only bundler, pinned exactly. The build is a repo script (`tools/build/build.mjs`) over esbuild's JS API, not a CLI line: it also emits `index.html` with hashed refs, the PWA manifest from `BRAND`, the generated token CSS and the service-worker precache list. `dist/` is **gitignored** and the build runs on every push. Dependency budget closed for MVP: runtime `preact`, `@supabase/supabase-js`; build `typescript`, `esbuild`. **Supersedes the committed-output half of option C** (chat 19): committing the bundle bought a silent failure class — source and served bytes drifting while everything looks fine — plus merge conflicts in generated code, a freshness gate and a byte-reproducibility requirement; build-at-deploy fails loudly instead. D197 stands, narrowed to what it was about: fonts and mascot are built rarely, reviewed by eye, binary and render-tested, so those outputs stay committed | option C as settled (commit `dist/`, CI freshness gate) / Vite with a dev server / no build step | MVP | two-way | Y |
| D213 | GitHub Pages, built by GitHub Actions from source. One vendor, no new account, and the deploy configuration is a workflow file in the repo rather than dashboard state, which is what harvest's untracked-infrastructure failure reintroduces. Every PR builds and publishes a preview at `/pr-<n>/`, and that build **is** G-BUILD. **The service worker registers only on the production path:** previews share an origin with production and an SW cache crossing that boundary is a day-costing failure; previews do not need offline. Site serves at `/hoopoe/`, with manifest `scope`, `start_url` and SW scope under it; a custom domain later is a DNS change. The bundle is public either way — the anon key is public by design and RLS is the boundary (§G). Does not reintroduce the reason Supabase's GitHub integration is disconnected: the workflow builds static output and never touches the database | Cloudflare Pages or Netlify (previews free, but build config lives in a dashboard) / no previews | MVP | two-way | Y |
| D214 | `strict` plus `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `noImplicitOverride`, `noUnusedLocals`, `noUnusedParameters`, `verbatimModuleSyntax`, `isolatedModules`. esbuild strips types and checks nothing, so `tsc --noEmit` is a **separate blocking gate** over all three projects: a green build proves nothing about types. `any` and non-null `!` banned. Every external payload — Supabase row, edge response, model JSON — crosses **one typed parse boundary** taking `unknown` and returning a result type, never a cast, which covers harvest's schema-drift and AI-output-parsing failures together. DB types generated by the Supabase CLI in a cloud session and committed at `src/data/database.types.ts`, command recorded beside it; no rebuild gate (CI holds no database credentials, §G), so instead a PR touching `supabase/migrations/**` without touching that file fails. Nothing outside `src/data/` sees a generated DB type | hand-written row types / `any` at boundaries with runtime checks only / a CI freshness rebuild of DB types | MVP | one-way (parse boundary) | Y |
| D215 | View layer is **Preact with TSX**. The deciding reason is singular: item-batched screens (D154) resolve each response inline while a sibling Arabic input holds focus and a caret, and D147 rides on exact keystrokes; a template-string re-render destroys that input, VDOM diffing keeps the node. Typed props also make D144's no-display-flag rule a compile error, but a typed factory function would too, so that argument is not load-bearing. esbuild compiles TSX natively, no plugin. Lit rejected: shadow DOM scopes styles we do not want (D143's three token layers are global custom properties, the duplicate-selector lint wants one CSS surface) and adds bidi and form-control risk around Arabic. Vanilla plus render helpers rejected: it is what `modular-rebuild-5` was. Hooks only, no state library. **Shared components expose no `class` and no `style` prop** — layout belongs to the parent's wrapper, which is what makes D143's `style=` ban survive a component library. Plain CSS files, no CSS-in-JS | Lit / vanilla plus typed render helpers / React | MVP | two-way (reversible while `ui/components` stays free of `data`) | Y |
| D216 | Repo layout `src/{lib,config,domain,data,ui/{tokens,components,screens,app},sw}` plus `assets/ shared/ supabase/functions/ tools/ docs/`. Import matrix: `lib`→nothing · `config`→`lib` · `domain`→`lib`,`config` · `data`→`lib`,`config`,`domain`,`shared` · `ui/components`→`lib`,`config`,`domain`,`ui/tokens`, **never `data`** · `ui/screens`,`ui/app`→everything above · nothing imports `ui` or `tools`. No barrel files; imports static, relative, explicit extensions; `import()` only at split points declared in the build script, which the lint reads. **No top-level `let`/`var` in `src/`** — module-level mutable state in engines is harvest's whole-state-sync failure — with block 2's single store module the one named exception. The D144 component reads through that store seam, not `data`. **Tokens are authored in one machine-readable source and the CSS custom properties are generated at build**, because D189 computes ratios from the token files and D143 lints for colour literals outside the primitive layer; both need parseable tokens. This is the one place a block-3 gate forces a block-1 decision | feature folders instead of layers / a `services/` layer split out of `data` / hand-written token CSS | MVP | one-way (layer boundaries) | Y |
| D217 | Three runtimes, closed list: browser (the whole app, a static PWA, no server of ours) · Supabase Edge Functions in Deno (C5.8 model policy, C5.10 prompts, gateway, `runtime_flag` reads — everything the client must not hold) · Node in a cloud session (`tools/`: build, lints, DR, authoring, never shipped). Edge-function source lives in `supabase/functions/` in this repo, since harvest's untracked-infrastructure failure is source not in the repo; who deploys is §H. No runtime code is shared between browser and Deno — `shared/` holds type declarations and constants only, type-checked by both | a Node server of our own / shared runtime utilities across browser and Deno / edge functions managed in the dashboard | MVP | two-way | Y |
| D218 | Block-1 gates, as a contract §H consumes; §E names each gate and what fails it, §H owns when it runs. **G-TYPES** `tsc --noEmit` × 3 · **G-TEST** `node --test` · **G-LINT** `tools/lint/`: import cycle, layer violation, unused export, barrel file, top-level `let`/`var`, empty `catch` or `.catch(() => {})`, `style=` in a component, duplicate selector, colour literal outside the primitive layer (D143), `learner_type ===` or `track ===` outside the resolver (C10.6) · **G-BUILD** the PR's preview build, so the preview is the artefact of the gate passing · **G-DBTYPES** migrations changed and `database.types.ts` did not. Contrast (D189) and font render (D194) are block 3. Unused-export roots are the build script's entry list plus test files. Hand-rolled lints are the reversible call: if `tools/lint/` passes a few hundred lines, ESLint replaces them | ESLint plus typescript-eslint from the start / no lint gates, rely on review | MVP | two-way | Y |
| D219 | Repo deliverables are diffs. The whole-file rule was correct when files were pasted into project knowledge by hand; inside the repo it burns tokens to produce something worse than the PR diff. The session edits the file, Marwan reviews the diff. The rule stays verbatim for anything applied by hand outside the repo: SQL for the browser SQL Editor, dashboard settings. **Amends D1** and the `CLAUDE.md` session rules | keep whole-file delivery everywhere / diffs everywhere including hand-applied steps | process | two-way | Y |
| D220 | Approval granularity is the slice, not the batch. Batch-by-batch build approval priced in an expensive, irreversible paste; a branch is cheap, revertible and now carries a preview URL that says more than an approval round. Spec-before-build is unchanged; only the granularity inside BUILD mode moves. Amends `build-protocol` | keep batch approval / drop approval gates entirely | process | two-way | Y |
| D221 | Gates replace self-check: any rule a machine can check becomes a gate and leaves the skill. `code-validation` keeps only what no gate covers (spec compliance, side-effect detection, hardcoded values) and drops undefined references, missing imports and syntax errors, which `tsc` does better, always, free. `build-protocol`'s DEBUG mode drops the mandatory full-codebase scan for a targeted one: it was written when the model could not grep. Amends `code-validation` and `build-protocol` | retire `code-validation` outright / leave both skills until §H | process | two-way | Y |
| D222 | A walking skeleton runs before §F. §E's gate is whether it can be built without a second judgement call, and text review catches contradictions but not an architecture that does not work. After blocks 2 and 3 are approved: shell, one screen, one Supabase read, one write, all five gates running, a preview URL on an iPhone. No features. The write targets a disposable `skeleton_probe` table with an anon-insert policy, dropped when §F lands, so **the skeleton proves the mechanism, never the schema**, cannot pre-empt §F, and explicitly does not prove the auth seam (§G). If it shows §E is wrong, §E is revised rather than ploughed past. Cost: one session, and §F starts later by that much. Amends `specification-first` | spec §F to §J first and build once / build a full vertical feature slice instead | MVP | two-way | Y |
| D223 | A block ends with its tracker rows, and the next block starts in a fresh session. The state file is what a cold session reads, so it has to be sufficient on its own; a fresh session that cannot proceed from the tracker is a tracker bug, fixed in that session before the block continues and named in the chat note. Carrying a block forward inside one long transcript lets it lean on reasoning that lives only in the transcript, which hides the defect instead of exposing it. Secondary and pointing the same way: a cold session pays one re-read and then runs lighter, and a review pass is sharper in a context holding one block's material than in one filtering three blocks of debate. Amends the `CLAUDE.md` session rules | keep a section in one session for continuity / split only when the context gets long | process | two-way | Y |
| D224 | Judging call settings: Opus 5 at effort `high`, system prompt sent as one cached block, call streamed with `max_tokens` 64,000, and stream decoding split into `stream.mjs` as an 11th module (`dr-runner-spec.md` §7.9). Temperature is not sent: it is removed on the model and returns 400 | temperature set explicitly / effort medium or low / Sonnet instead of Opus / a single 214-line `judge.mjs` | MVP | two-way | Y |
| D225 | Staging conflicts do nothing; they do not merge (`dr-runner-spec.md` §7.8). The build brief calls the write an "upsert", but §5 is the behaviour and resume depends on it. A genuine re-judgement takes a new `run_id`, so both judgements survive | merge-duplicates as the default | MVP | two-way | Y |
| D226 | `DR_ANTHROPIC_KEY` is rotated at the promotion gate (`dr-scoped-pass.md` §2), not "later": an env var is readable by anyone using `hoopoe-dr`. Attached to a named gate rather than a list, per that table's own rule | rotate after the full loop, unattached / leave it, solo project | MVP | two-way | Y |
| D227 | A ~10-row smoke batch runs before the 60-row pilot, checked against P1-P4 only, because those four are shape checks that fail identically at either size. It discharges no part of the pilot gate | go straight to the 60-row pilot / treat the smoke batch as partial P-gate evidence | MVP | two-way | Y |
| D228 | Spend cap `spendCapUsd` is **$2.50 for the pilot and $20.00 for the full loop**, closing Q17 and unblocking B3's stop conditions. `config.mjs` keeps refusing a default, so the figure is passed by the caller and never hardcoded. The cap is a halt, not a warning, and hitting it is safe rather than costly: resume keys off `(run_id, dictionary_id)`, so a capped run continues by raising the figure and re-running, with no rows re-judged. That makes $20 a checkpoint against the $16-30 single-row estimate rather than a ceiling that loses work | a cap above the estimate's top so the loop cannot halt / no cap, watch the bill / a default inside `config.mjs` | MVP | two-way | Y |

## Bug history
One line per fixed bug, newest first: symptom, root cause, fix, prevention rule. Recorded only after the fix is merged. A prevention rule that generalises beyond Hoopoe goes to the relevant skill instead (skill-evolution).

- **Prompt contract never reached the model (B2, chat 22).** Symptom: the first live model call returned well-judged rows in an invented shape — `confidence` as a nested object, an invented `pos_reason`, `corrections` as an object — and `validate.mjs` rejected every field. Root cause: `dr-prompt-scoped.md` keeps the output contract outside both fenced blocks, as frozen `dr-prompt.md` does, and `prompt.mjs` injected only the two fences, so the model was never told the shape to return. Fix: `buildBatchMessage` appends the doc's own contract section to the user turn verbatim, and a doc with no such section is now a hard error. Prevention: generalises past Hoopoe, so per this section's own rule it went to `test-driven-development` rather than staying here (chat 22) — a module whose output is consumed by something outside the test suite is not done on green tests alone.

## Refactor history
One line per merged refactor, newest first: before, after, the pattern it establishes. Behaviour-preserving by definition; anything that changes behaviour is a spec decision and belongs in the decision log.

(none recorded)

## Open questions → topic (harvest §10c unless noted)
- Q1 Framework, tooling; native-app path (D11) → E. Repo half closed by D136; **tooling half closed chat 22 (D212-D218)**. Native-app path stays open to §E block 3
- Q2 Auth: magic link, invite-only, guest → G
- Q13 Sign-in method on installed iPhone app (magic link vs email code) → G
- Q16 Content and dictionary edit versioning → F (mechanism named in C5.11, schema open)
- Closed chat 22: Q17 spend cap → D228 ($2.50 pilot, $20 full loop)
- Q18 Batch API transport, 50% cost and async → DR, after the pilot by decision (chat 22). It cannot pay on a single batch
- Q19 Edge-function transport, still stubbed → DR, before the full loop (`dr-runner-spec.md` §11, credential reasons). Changes no per-token cost, but its execution timeout may constrain batch size. Best decided with Q18
- Closed: Q3 (D114 + D115), Q4 (D118 + D119), Q5 (chat 4), Q6 (D20), Q7 (D47), Q8 (D72 + D78 + D82), Q9 (D46 + D59), Q10 (D53), Q12 (D93)

## DR: dictionary review + `level`
Content work, not a spec section. Blocks: J placement bank build, J catalogue build, Phase 2 package.
**Specced chat 12.** Sources of truth: `dr-spec.md` (construct, rubric with real anchor ids, enums, `formula_pair`, acceptance floors F1-F7), `dr-prompt.md` (frozen batch prompt, JSON contract, batching, deterministic rule-fix pass), `dr-runbook.md` (staging DDL, run order, gates G1-G8, promotion SQL). The 2026-09-11 review prompt draft is retired: it used `rank` as a frequency proxy and batched by category.
- Columns to add: `level` 1-5 (C2, D30); `arabic_vocalised` (C9, D103) — needed by both the C4.8 harakaat rung and TTS input, free to add before the review runs.
- Romanization: normalise every row to the D47 Arabizi standard; the `romanization_map` freezes at the end of DR (C4.7).
- `pos` normalised to a fixed enum (C5: distractor selection depends on it).
- Build a `function_words` allowlist table (C5.5: the known-pool and dictionary-ref validators need it to be strict).
- Track tagging resolved (D121): `track_id` backfills to `palestinian` on every row, no per-row judgement.
- `dialect_tag` normalised to a `register` enum (D121); C2.2 filters on it.
- Category names freeze at end of DR: the C3 unit catalogue references them (D32).
- Batching is stratified random sample (~120 rows), not by category: `level` is global, so category batches anchor to their own spread and drift. Exceptions batched as whole sets: the 272 social-formula rows (pairs need both halves in view) and the closed sets (gap detection).
- All `pair` claims are held for human review regardless of model confidence. A wrong pair propagates into item sets, levels and checkpoints.
- Write policy (Marwan, chat 12): high-confidence judgements auto-apply at promotion, per field not per row; M/L and anything `native_check` goes to the adjudication worksheet. Live `dictionary` is never written during the pass.
- Gap cross-check against a third-party essentials list is manual, by Marwan as a subscriber (D131). No credentials handled, no list ingested.
- Opus calls in the batch runner route through the existing `claude` edge function rather than an Anthropic key in the environment. Its source was never committed, so it has to be downloaded from the old project and deployed to the new one. An Anthropic key as an environment variable is acceptable for the pilot batch only (readable by anyone using the environment, which on a solo project is Marwan); the edge function is required before the full 2,728-row loop.
- Target is the new project (D137): DR runs against `pniwgnjljpkiimssortp`. **Done chat 14 (D139):** schema applied by hand in the browser SQL Editor and recorded as a full replayable recipe at `supabase/migrations/20260912135736_dr_schema.sql` (base `dictionary` table, DR columns, 3 DR tables, RLS, grants, idempotent); rollback snapshot at `tools/dr/snapshots/dict-preDR.json`; 2,728 rows migrated and hash-verified. `conjugation` confirmed `text`, not jsonb.
- **Runner specced chat 15** (D140): `docs/dr/dr-runner-spec.md`. Module map, model seam, batch plan, resume, stop conditions, tests, and the 7 resolutions where `dr-runbook.md` and `dr-prompt.md` could not be executed as written. Pilot is 1 sampled batch gated on P1-P8; F1-F7 stay post-pass floors. Build is code in batches, pilot first.
- **Scoped chat 16** (D141): `docs/dr/dr-scoped-pass.md` and `docs/dr/dr-prompt-scoped.md`. Collected in the one pass: `level`, `romanization`, `arabic_vocalised`, `pos`, `register`, `form_origin`, flagged `corrections`, `native_check`. Deferred with gates: corrections triage and held-row adjudication (worksheet, any time); `pair` and `constituents` (second pass, before any release beyond the family test); native spot check (40 rows sampled from `review_confidence = 3`, **in front of** the family test); F1-F7 and the D131 gap cross-check (after the second pass). `review_confidence` 1-3 and `native_verified` added to `dictionary` (`supabase/migrations/20260912163000_dr_confidence_columns.sql`); score is the minimum across `level`, romanization, vocalised and the enum triple, capped at 2 by any flagged correction on meaning, harakaat or romanization. Runner deltas: scoped prompt file, no grouped batches, no pair routing rule, default batch size 60, new P9 for the `review_confidence` distribution. Legacy `confidence` column retired as a selection signal. What this does not buy: verified accuracy — a confidently wrong row scores 3, which is what the 40-row check exists to catch.
- **Romanization map merged to `main` (chat 18).** `docs/dr/romanization-map.md` is the single source of truth for the D47 standard during DR and supersedes the 10-symbol sketch in C4.7. One glottal symbol `2` for hamza and urban ق alike; ض `9'`, ظ `6'`, apostrophe as emphatic modifier only; five long vowels (`beet` and `biit` contrast, never collapse); taa marbuta by environment; `il-` hyphenated and assimilating; digraph breaker `-`; allowed set `a-z`, `2 3 6 7 9`, `'`, `-`, lowercase only. Amends C4.7; knocks on to C4.6 (hyphen costs distance 1, tolerance absorbs it), D147 and D185. **Header corrected chat 21** (it cited D142, the dark-theme decision, where D141 is meant). **One fix still owed:** the presentation rules in D185 assume its character set, which should be re-checked if the map changes before the freeze.
- **Vowel length resolved (D210, chat 19).** The pre-DR data conflates `oo`/`uu` and `ee`/`ii`, so no rule recovers it and the don't-flag-what-a-rule-fix-covers instruction was suppressing the field. It is now a judgement inside the produced `romanization` value, explicitly carved out of that instruction and explicitly not a `corrections` entry, because a flagged romanization correction caps `review_confidence` at 2 and would have taken most of the dictionary out of the MVP pool. `rulefix.mjs` is barred from touching the vowel symbols; P10 flip rate added to the pilot gate; the spot check samples at least 20 long-vowel rows. `dr-prompt-scoped.md` and `dr-scoped-pass.md` updated. **B4 unblocked.** Residual risk sits in R7: nothing verifies vowel length mechanically, since the script does not disambiguate it either.
- **Three consistency fixes, chat 21.** No decisions. `dr-build-brief.md` carried P1-P9 in B3 and B4 where D210 had added P10, so a B3 session would have built a report missing the gate that catches prompt pass-through; the brief now reads P1-P10 and states P10's consequence (fix the prompt, re-run the pilot). `dr-scoped-pass.md` §6 claimed the vowel-length bar was "asserted in its fixture tests" and no such assertion existed, so `rulefix.mjs` satisfied D210 only by having never touched vowels; two tests now assert it (long-vowel symbol sequence preserved on every fixture row, and romanization changed only by whitespace collapse), on a new synthetic fixture row id -3 carrying `oo` and `ee`. 43 `node:test` cases pass, up from 41. B2's live-call precondition named in the brief: `api.anthropic.com` on the Custom network list plus the key, and a session missing either satisfies the dry-run half and stops.
- **B2 built and merged (chat 22, PR #8).** `db.mjs`, `judge.mjs` and `stream.mjs` plus offline suites; 74 `node:test` cases pass. The dry run read all 2,728 rows and staged nothing; a live single-row call validated and routed to `review_confidence` 3, carrying the vowel-length flip P10 looks for (`mabrook` to `mabruuk`). Both `hoopoe-dr` preconditions were already in place. Full-pass cost estimated at $16-30 and the pilot about $0.50, but from single-row calls, which over-represent per-call overhead: **P8 replaces that estimate rather than confirming it.** D224-D227 and the first bug-history entry come from this build. Spec files changed on `main` with no tracker action: `dr-runner-spec.md` §2 §3 §4 §7.8 §7.9, `dr-scoped-pass.md` §2, `dr-prompt-scoped.md` header and contract heading, `dr-build-brief.md` B2-B4 plus a B3 opening prompt.
- **Tier 0 applied and verified (chat 18).** `dictionary` gains `review_confidence` (int, nullable, 1-3 constraint, indexed) and `native_verified` (boolean, default false), plus a `service_role` grant, recorded unchanged at `supabase/migrations/20260912163000_dr_confidence_columns.sql`. Verification returned 2,728 / 0 / 0 / 2,728: 2,728 rows, 0 scored, 0 verified, `track_id = 'palestinian'` on every row per D121. All six named gates unchanged. R7 unchanged, since it turns on a `review_confidence` distribution the pilot has not produced.
- Edit rule: in-place row edits only; small corrections allowed anytime. No delete + re-import (reassigns ids, re-ranks, breaks references). Edit versioning → Q16, F.
- Post-publish edits propagate: a change to `arabic`, `romanization` or `arabic_vocalised` marks dependent content for revalidation and dependent clips for regeneration (C5.11, C9.1).

### DR tooling (chat 11)
The bulk pass is a batch job, not a conversation. Turn limits stop mattering once state lives on disk and in the database.

| Stage | Where | Why |
|---|---|---|
| Freeze the DR prompt, JSON schema and acceptance rules | Chat, Opus | Small, high leverage, no context pressure |
| Batch runner over 2,728 rows into a `dictionary_review` staging table | Claude Code | Resumable by row id, logged, cost-metered; the model doing the linguistic judgement is set by the API call inside the script, so a Sonnet-orchestrated session can call Opus per batch |
| Adjudicate the flagged minority: low confidence, romanization disagreements, ambiguous `level` | Cowork or chat | Judgement work on a worksheet; small volume |
| Native spot check | Human, on a sampled worksheet | |
| Promotion | One SQL step, in-place UPDATE | Staging keeps the originals, so the pass is reversible and the reviewer gets a diff |

Staging table is mandatory. The live `dictionary` is never written to directly during DR.

**Cloud environment `hoopoe-dr` (chat 13).** The Default environment reaches GitHub, `api.anthropic.com` and package registries only, so Supabase calls fail.
- Network access **Custom**: `pniwgnjljpkiimssortp.supabase.co` and `xkhulybdrxdzakarivvi.supabase.co`, both, because the migration reads one and writes the other, plus `api.anthropic.com` for any session that calls the model directly (B2 onward). Custom replaces the default allowlist, so a host absent from it is unreachable even though the Default environment reaches it. Keep the default package-manager list.
- New project's `service_role` as a stored **API credential** scoped to the new host, so the session never sees the key. Added by editing an existing environment, not at creation.
- **Two headers on one credential (chat 14).** Supabase's gateway requires `apikey` on every `/rest/v1/` call, separately from `Authorization`, and no credential *type* in the dropdown supplies it. Use the Custom headers rows: `Authorization` with prefix `Bearer`, and `apikey` with the prefix field **empty**, both holding the same key. Check the resolved curl example before saving. Existing credentials cannot be edited, so a fix means creating a replacement and deleting the old one.
- Old project needs no credential: RLS is off and its anon key is public in `arabic-app`'s `js/config.js`.
- Anthropic key as an env var for the pilot batch; the `claude` edge function route before the full loop. The key alone is not enough: `api.anthropic.com` has to be on the Custom network list too (found chat 21, B2's live-call precondition).
- **Reading the failure (chat 14).** 401 `UNAUTHORIZED_MISSING_API_KEY` with `X-Proxy-Error: upstream auth failed` is the missing `apikey` header, not a Supabase setting. 403 `42501 permission denied` is grants: tables created by hand in a new project do not inherit `service_role` privileges. Fix in the SQL Editor with `grant all on table ... to service_role;` plus `grant usage, select on all sequences in schema public to service_role;`, then `select has_table_privilege('service_role','public.dictionary','INSERT');` to confirm.

## Queued for §J manual actions
- Audio spike: 20 clips (10 word clips at `level` 1-2, 10 dialogue lines across both voices), native listen, blocks the corpus run and settles D105 (chat 10).
- Rotate the ElevenLabs key sitting unrotated in git history (harvest §6f). **Outstanding since chat 13; ten minutes at the provider.**
- Apply D211 to the plugin copy `anthropic-skills:test-driven-development`, which still carries the deleted ladder, and D219-D222 to the plugin copies of `build-protocol`, `code-validation`, `specification-first` and `skill-evolution`, all four now edited in this repo. Skills are per-repo copies, so a repo fix does not reach them. **This queue must not grow past these: every skill edit generates a plugin-copy chore.**
- Enable GitHub Pages on `mharb92/hoopoe` (repo Settings → Pages, source: GitHub Actions) before the first deploy workflow runs (D213).
- Delete the open `ELEVENLABS_FUNCTION_URL` edge function rather than porting it; the runtime route is a new gateway path under §G (chat 10).
- Confirm live per-character pricing against the C9.9 volume estimate.
- Export the 20 `personal_vocab` rows from the old project before it is paused, so §F has a shape to re-import from.
- Download the `claude` edge function from the old project and deploy it to the new one. Source was never committed. Blocks the full DR loop, not the pilot batch.
- Pause the old Supabase project `xkhulybdrxdzakarivvi` after DR promotion.
- Archive `mharb92/arabic-app` read-only after DR promotion.

## Parked
- Universal harvest §8 lessons as section edits to the general skills during §H (remainder of the chat 2 skill plan; the rest of it closed in chat 20). **Narrowed chat 22:** the subset that D219-D221 contradict outright comes out of the park and lands in its own PR now, because a skill saying the old thing between here and §H is the chat 21 pattern — a decision landing in one file and not its dependents. The rest of the §8 pass stays parked.
- Adaptive review re-weighting as an explicit mechanism (chat 6): not built. C6 due dates already produce heavier review after heavy days; revisit only if the family test shows the emergent behaviour is insufficient.

## Chat notes
- Chat 22: §E block 1 approved (D212-D222). Two settled things were reopened on Marwan's questions and both moved. **Committed `dist/` is gone:** option C's committed-output half was reasoned when there was no terminal anywhere, and both premises expired — the build now runs in a cloud session that reproduces the host's build exactly, and a per-branch preview URL gives the real app on a real iPhone before merge. Committing derived bytes was buying a silent drift class where build-at-deploy gives a loud one; the R9 drafted for that risk was withdrawn rather than recorded, because removing the mechanism removed the risk. D197 survives, narrowed to what it was actually about: assets built rarely and reviewed by eye stay committed, the bundle does not. **Hosting is GitHub Pages, not a second vendor:** the recommendation flipped mid-session on the argument that a dashboard holds untracked config, which is harvest's own named failure, while a workflow file is reviewable in the same PR as the change it deploys. Preview plumbing is the price, and the SW-across-origins hazard it carries is mitigated by registering the service worker on the production path only. Three chat-era protocol rules changed (D219-D221) and a walking skeleton was added before §F (D222); Marwan's point that skills contradicting the new protocol would mislead every session until §H was correct, so the contradicting subset leaves the §H park and lands in its own PR. Two review findings worth keeping: the Preact case had been carried by an argument a typed factory function satisfies equally, so the justification was narrowed to the one reason that separates the options; and PR previews write to the production Supabase project, which D222's probe table covers for the skeleton and §G owns in general. **D219-D223 applied the same session, in their own PR:** `CLAUDE.md` session rules, `build-protocol` (slice not batch; targeted scan replacing the mandatory full read, and the Emergency Override section deleted with it since it existed only to guard that read), `code-validation` (everything `tsc` and the lints cover removed, and the non-developer strictness ladder deleted as D211 deleted the same thing from `test-driven-development`), `specification-first` (walking skeleton) and `skill-evolution`, whose "never a diff" step was a direct dependent of D219 and would otherwise have been the next chat-21-pattern gap. Standing flag: §E is necessary and is not the critical path — DR has not run, so there is no reviewed dictionary and no catalogue for a correct architecture to serve. **DR B2 built and merged in parallel (PR #8), producing D224-D227 and the first bug-history entry** — B2's four decisions were handed over numbered from D223, which this session had already taken for the block-boundary rule, so they were renumbered D224-D227 rather than renumbering a decision already written into `CLAUDE.md`. The B2 bug is the same shape as chat 21's pattern in a new place: offline tests over a prompt builder pass whether or not the prompt states its contract, so the contract was missing from the live call and nothing could catch it before one ran. Next: §E block 2 in a fresh session per D223, then block 3; DR B3 in parallel, blocked on Q17.
- Chat 21: no spec sections, no decisions. Four consistency gaps found on reading the state and closed: the `romanization-map.md` header D142→D141 (§J queue item, now removed); `dr-build-brief.md` B3/B4 P1-P9→P1-P10 after D210; the D210 vowel-length bar asserted in `rulefix.mjs` tests, which `dr-scoped-pass.md` §6 already claimed existed (43 cases now pass, from 41); and B2's live-call precondition recorded, `api.anthropic.com` on the `hoopoe-dr` Custom network list as well as the key, since Custom replaces the default allowlist rather than adding to it. The pattern worth keeping: two of the four were docs asserting something the code did not do, both introduced by a decision landing in one file and not its dependents. Next: §E block 1 and DR B2, in separate parallel sessions.
- Chat 20: no spec sections. Migration off claude.ai project knowledge to Claude Code cloud sessions against `mharb92/hoopoe` @ `main`. Project knowledge moves to `docs/`; the `arabic-learning-app` stub is retired and replaced by a repo-root `CLAUDE.md` carrying status, sources of truth, repos, Supabase projects, session rules, principles gate, retired list and the `hoopoe-dr` environment. Eight general skills committed verbatim to `.claude/skills/`, `skill-evolution` rewritten so a convention change is a file edit and a PR; the per-repo fork is accepted. `project-memory-protocol`, `marwan-writing-style` and `bva-builder` not migrated, so this file gains bug history and refactor history sections. The Opus-in-chat / Sonnet-in-Code split is retired: Opus runs in Code. Handover docs stop existing; `handover-chat20.md` is archived at `docs/archive/` and this file is the state file. Session rules deleted from here and left only in `CLAUDE.md`, because keeping them in both is what put them out of step.
- Chat 19: no spec sections. D210 settled, which unblocks DR B4; `dr-prompt-scoped.md`, `dr-scoped-pass.md` and R7 updated. The conflict was narrower than it looked: the pass already produces `romanization` wholesale, so vowel length only needed moving out of the rule-fix suppression and kept out of `corrections`. Keeping it out of `corrections` is the load-bearing half, because the cap at 2 would have emptied the `review_confidence = 3` pool. What the resolution does not buy is verification: the script does not disambiguate و or ي by length, so `arabic_vocalised` cannot cross-check the romanization and P10 detects pass-through rather than error. §E opened: block shape approved (1 stack and structure, 2 runtime shape, 3 build-time and portability, then the §D-style internal review, then the principles check) and three boundary calls made (§E owns access mechanism and §F owns record shapes; §E writes the build gates as a contract §H consumes; auth, gateway and RLS are §G and §E defines only the client seam). Stack direction settled as option C: TypeScript and a build, produced in a cloud session with the output committed beside source per D197's logic, plus a CI freshness gate that rebuilds and fails on a diff. Deliberately not given a D number: it lands as one when §E.1 is drafted with the view layer and module boundaries beside it. Next: §E block 1, and B2 in a cloud session in parallel.
- Chat 18: §D blocks 3 and A-bis approved, §D closed (D164-D209). Progress and Settings, system states, copy and tone, accessibility, assets, then an internal review of D142-D197 before the principles check. The review found three contradictions and three absent screens, which is the same failure mode as block 2: gaps surface on review, not on drafting. D164 sent two of Home's four figures nowhere; three D186 specimens failed D178, which turned out to be too strong rather than the copy being wrong; the script rungs had no names and a correct answer had no defined mark (D198, D199). Absent and specced as pass A-bis: onboarding (D200-D203), wrap-up as a screen after results rather than merged into it (D204-D206), alphabet (D207-D209). Marwan's ranking-system idea resolved into D172 and D173: the motivating part of a level ladder is the unlock, which Hoopoe already has, so what files to §B6 is status-by-capability plus a CEFR-descriptor proficiency review, with three nullable fields shaped now so it is a display change later, and D174 records the §I proof obligation rather than leaving the slot aspirational. New risk R8 in `principles.md`: the typed-answer toggle lets P14's withdrawal operate inside a learner-set ceiling. B1 merged to `main`, so `docs/dr/romanization-map.md` is live and ratified three D185 rules while surfacing three the draft had missed. `arabic-learning-app` skill updated (DR sources of truth, build ref, status). `PROJECT_SPEC.md` §D written, `spec-tracker.md` rebuilt. Next: §E Architecture, and B2 in a cloud session in parallel.
- Chat 17: §D blocks 1 and 2 approved (D142-D163). Foundations, Arabic rendering, input and navigation, Home, the activity frame, placement, checkpoints, side surfaces, report-a-problem and audio. §D is the first section whose deliverable is screens, so the gate became whether a screen can be built without a second judgement call; two things failed it on review rather than on drafting — the first activity-frame design could not render any item-batched activity (D154 revised), and report-a-problem was missing entirely despite C1.6 requiring it everywhere (D162). `arabic-learning-app` skill updated in-chat for the scoped DR pass, the `D.n` convention and the C11 trigger. Next: §D block 3.
- Chat 1: A and B approved. Next: C1.
- Chat 2: C1 approved, D15 overridden to unlimited pacing. Next: C2.
- Chat 3: C2 approved (D20-D30); rank found to be batch order, bands moved to `level`; DR step added. Next: C3.
- Chat 4: C3 approved (D31-D41). Q5 closed. C1.1 stages 5 and 9 amended; C1.7 C3 line removed. Next: C4.
- Chat 5: C4 approved (D42-D52). Q7 closed, Q9 narrowed. DR gains romanization normalisation. Next: C5.
- Chat 7: C6 approved (D72-D84). Q8 closed. Mastery model replaced: box + `due_on`, decay retired; `known_gaps` lands as `status = gap` on `learner_item`; C1.7, C3.5, C3.12, C4.12, C5.12 and B8 deferrals all discharged. Remedial triggers declared by drills, so DR is unchanged. Next: C7 Quizzes.
- Chat 9: C8 approved (D93-D100). Q12 closed. D98 revised from the proposal: confusion ids are recorded, not wired into the remedial counter. Amends C1.7 and C3.12 (anchor pointers discharged) and C6.14 (checkpoints move no boxes). New recorded risk R4 in `principles.md`: runtime checkpoint language cannot be validator-gated. DR unchanged; `arabic-learning-app` skill unchanged (pointer stub). Next: C9 Audio.
- Chat 13: infrastructure, no spec sections. D136-D138. `mharb92/hoopoe` created, 8 files committed (`README.md`, `CLAUDE.md`, `.gitignore`, `docs/dr/{dr-spec,dr-prompt,dr-runbook}.md`, `docs/setup/supabase.md`, `tools/dr/.gitkeep`); new Supabase project created and still empty. Work laptop found to be browser-only, so all terminal work moves to Claude Code cloud sessions. `docs/setup/supabase.md` is stale CLI guidance, to be rewritten or deleted. Open: confirm the push landed, create `dictionary` + the DR schema in the new project, configure the `hoopoe-dr` cloud environment, migrate the 2,728 rows, then spec the DR batch runner (build, needs approval). `arabic-learning-app` skill updated (repos, Supabase projects, cloud environment, browser-only). Next: the migration, then §D UX.
- Chat 16: DR scoped, no spec sections. D141. Marwan flagged that four consecutive infrastructure chats had produced no spec progress, which was accurate: §D-§J are what block the MVP, not the dictionary, and they need DR's schema decisions (made chat 12) rather than its values. Pass reduced to one unattended machine run; every human queue deferred and attached to a gate. `review_confidence` 1-3 added to `dictionary` on Marwan's proposal — a better design than routing in staging alone, because re-review upgrades rows in place and widens the MVP pool without republishing. Collect-wide/defer-the-queues replaced the earlier cut-by-field framing: re-collecting a field costs a second full pass, so `register`, `form_origin` and flagged `corrections` came back in. Amends C2.2 and C5.5; new risk R7 in `principles.md`. Named gates: native spot check moves in front of the family test, 40 rows sampled from the 3s; pairs and constituents before any release beyond the family test; a second `level` pass only at a phase boundary (C10.7). `arabic-learning-app` skill needs a DR sources-of-truth update. Next: Tier 0 SQL, B1-B3 in cloud sessions, pilot, and §D UX in chat in parallel.
- Chat 15: infrastructure, no spec sections. D140. DR batch runner specced, approved and committed to `docs/dr/dr-runner-spec.md`. Three points in the frozen DR docs proved unexecutable as written and were resolved in the runner spec rather than by amending them: the runbook's rule-fix step and model step both insert every row against `unique (run_id, dictionary_id)`; per-field routing cannot be expressed in one `status` column without breaking the frozen promotion SQL; the 272-row grouped batch exceeds any single response. `arabic-learning-app` skill updated (DR sources of truth). Next: build the runner in batches, pilot batch first.
- Chat 14: infrastructure, no spec sections. D139. Dictionary migrated to the new project and gate-verified (2,728 rows, hash match), sequence resynced, DR schema recorded as a full replayable recipe in `supabase/migrations/`, `docs/setup/supabase.md` deleted, cloud work merged to `main` and the working branch deleted. Two failures worth keeping: a Bearer-only credential cannot reach PostgREST (needs a second `apikey` header), and hand-created tables in a new Supabase project have no `service_role` grants. `arabic-learning-app` skill updated (Supabase projects, cloud environment, credential pattern). Next: spec the DR batch runner (build, needs approval), then §D UX.
- Chat 12: DR specced, not run. D127-D135. `principles.md` P10 reworded (first edit to the standing set since chat 6). Confirmed against live data: `rank` is unusable as frequency (بيت 1091, مي 1046, سيارة 1285); `dialect_tag` legend reconstructed from `notes` (there was none) and found to conflate form provenance with register; only 30 rows encode a call-and-response anywhere, so `formula_pair` is authoring work over ~272 rows, not extraction. Amends C2.2 (source filter, formula exclusion, band definition), C4.6 and C7 (distractor rules), C5.5 (pair validator, constituent resolution, 3-regime known pool), C6.12 and B8 (split counters, phrases never credit words learned), C11.1 (level exemption), B5 and C11 progress lines. §F obligations recorded under "D to J". `arabic-learning-app` skill unchanged (pointer stub). Next: §D UX.
- Chat 11: C10 approved (D113-D121), C11 created and approved (D122-D125), retro-audit A1-A7 applied (D126). Q3 and Q4 closed. Audit found 3 contradictions in approved sections: the C7.2 quiz range was arithmetically impossible for heritage L3 (6 items + pattern + 2 unit recall = 9 against a 4-7 range), the C4.2 activity budget and the C4.3 exposure floor were unsatisfiable together, and C9.7's swap-to-script would have corrupted the placement ceiling. Also: C5.4/C5.5 became one publish gate for all artefact types, dictionary edits now propagate to dependents, report triage named as §H. Retro principles checks added to C1-C4. New recorded risk R6. DR gains `track_id`, `register` and the staging rule. `arabic-learning-app` skill unchanged (pointer stub). Next: §D UX.
- Chat 10: C9 approved (D101-D112). D67 discharged. Audio moves off the runtime path entirely: pre-generated, content-addressed clips served as static assets, 1 runtime surface (checkpoint turns). Web Speech retired; the old fallback is what hid the dead ElevenLabs pipeline for months. Amends C2.7 (D67 consumers), C4.8 (vocalised source) and C5.4 (stage 3 names TTS). DR gains `arabic_vocalised`. New recorded risk R5 in `principles.md`. 4 items queued for §J. `arabic-learning-app` skill unchanged (pointer stub). Next: C10 Tracks (Q3, Q4).
- Chat 8: C7 approved (D85-D92). C7.6 and C7.7 amended after review: the learner sees the per-lesson count and per-item states, and gets unscored practice-again on missed items; the threshold stays internal. Amends C5.5 (modality assertion), C6.4 (box 0 entry on a below-threshold quiz) and C6.5 (not-yet-due unit recall rule, unscored attempts). DR unchanged; `arabic-learning-app` skill unchanged (pointer stub). Next: C8 Checkpoints (Q12).
- Chat 6: C5 approved (D53-D62). Q9 and Q10 closed. Retro-audit of B-C4 against learning principles found 7 gaps → D63-D71 amend C1.2, C2.7, C3.2, C3.4, C3.5, C3.6, C4.3, C4.6 and B2. `principles.md` created with an 18-principle set, 3 recorded risks and a standing section gate. DR gains `pos` enum + `function_words` allowlist. Next: C6 Mastery (Q8, plus D64, D66, D71 land there) — done chat 7. D1 clarified: deliverables are complete files, never edit instructions.
